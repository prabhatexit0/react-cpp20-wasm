# Dependencies
node_modules/

# Build outputs
dist/
build/

# Note: WASM artifacts (engine.js + engine.wasm) are committed so that
# Netlify and other static hosts can build the frontend without Emscripten.
# To update them, run: npm run wasm:rebuild

# Editor
.vscode/.cache/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local
